# cn.ts

- **Que hace**: Utilidad para combinar clases CSS con tailwind-merge (evita conflictos)
- **Datos**: Exporta `cn(...classes)` que mezcla clsx + twMerge
- **Dependencias**: Usado en practicamente todos los componentes
- **Pitfalls**: Ninguno — utilidad pura sin estado
